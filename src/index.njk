---
layout: base.njk
title: Home
---

<div class="homepage">
    <section class="hero">
        <h1>Welcome to Pete's Blog</h1>
        <p>Thoughts on technology, development, and whatever else comes to mind.</p>
    </section>

    <section class="recent-posts">
        <h2>Recent Posts</h2>
        {% if collections.posts.length > 0 %}
            <div class="posts-grid">
                {% for post in collections.posts | limit(6) %}
                <article class="post-card">
                    <h3><a href="{{ post.url }}">{{ post.data.title }}</a></h3>
                    {% if post.data.summary %}
                        <p>{{ post.data.summary }}</p>
                    {% endif %}
                    <div class="post-meta">
                        <time>{{ post.data.date | readableDate }}</time>
                        {% if post.data.primary_topic %}
                            <span class="topic">{{ post.data.primary_topic }}</span>
                        {% endif %}
                    </div>
                </article>
                {% endfor %}
            </div>
        {% else %}
            <p>No posts yet. Check back soon!</p>
        {% endif %}
    </section>
</div>